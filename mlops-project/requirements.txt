# MLOps 프로젝트 핵심 라이브러리 목록 (Python 3.8 호환 버전)

# 데이터 처리 및 분석
pandas==2.0.3
numpy==1.24.4
scikit-learn==1.1.3

# 데이터베이스 연결
psycopg2-binary==2.9.9

# 모델링 및 실험 관리
mlflow==2.6.0
boto3==1.28.66

# Feast와 그 의존성 (Python 3.8 호환)
# Feast 0.20.0 버전을 사용합니다.
feast==0.20.0
# Feast의 의존성인 grpcio, google-cloud-bigquery 등을 함께 명시합니다.
grpcio==1.50.0
google-cloud-bigquery==3.10.0

# 모델 서빙 (FastAPI + Uvicorn)
fastapi==0.95.2
uvicorn[standard]==0.22.0